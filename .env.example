# Postgres (로컬 Node 서버 / db:check 시 사용)
# docker-compose 띄운 뒤: npm run db:up && npm run db:init && npm run db:check
# 나중에 Cloudflare Hyperdrive 연결 시 Worker에서는 바인딩 사용
DATABASE_URL=postgresql://onewave:onewave@localhost:5432/onewave

# OAuth2 + JWT (Google 로그인)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
OAUTH_REDIRECT_URI=http://localhost:5174/v1/auth/google/callback
JWT_SECRET=your-32-char-or-longer-secret-key
# SPA 로그인 후 토큰 전달용 (선택). 설정 시 callback 성공 후 이 URL로 리다이렉트 (#token=...&internal_id=...)
# FRONTEND_REDIRECT_URI=http://localhost:5173/auth/callback

# 단어장 생성 (POST /v1/vocabulary/generate): 노래 제목 → Genius 가사 → Gemini 단어 추출
# SECRET_GEMINI_API_KEY=   또는 GEMINI_API_KEY=
# SECRET_GENIUS_API_KEY=  또는 GENIUS_API_KEY=  (Genius API 토큰)

# Wrangler(npm run dev) 로컬 개발 시: 위 변수들을 .dev.vars 파일에 그대로 넣고,
# OAUTH_REDIRECT_URI는 http://localhost:8787/v1/auth/google/callback 로 맞춘다.
